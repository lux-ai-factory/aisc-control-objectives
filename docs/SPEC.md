# Control Objectives to Select Tests & Datasets — Specification

Status: **draft for review** — see §12 Open questions before implementation starts.

## 0. Hard constraint: strictly additive (v1)

The service must not modify any existing module — no code edits, no schema
migrations, no config changes in `aisc`, `catalogue2`, or the demo. Concretely:

- **Own repo dir** (`~/control-objectives`), own compose file, own port (`:8090`). It joins
  the existing docker network as an *external* network — the aisc compose files
  are not touched.
- **No Caddy route.** Served directly on `http://localhost:8090` (same pattern
  as catalogue `:3000` and dashboard `:3300`). Moving behind Caddy at `/control-objectives`
  is a refactor-later item.
- **Read-only against existing data.** Qualification and catalogue are read via
  their existing HTTP APIs; controls via a read-only Postgres role. The only
  write into an existing system is the export step, which inserts *data rows*
  (controls `Submission` drafts — the exact pattern the demo seed already
  uses) and never alters schemas.
- **Own database** (`control objectives`) on the existing Postgres instance, created by the
  control objectives's own setup script.
- Homepage link-up is a one-line edit in the demo repo, done separately when
  you choose (the service card currently has no link, so nothing breaks
  meanwhile).

Refactor-later list (explicitly out of scope now): Caddy `/control-objectives` route,
read-only REST routes inside the controls app, shared tag-taxonomy package.

## 1. Purpose

The service takes a completed **qualification** of an AI system and produces a
reviewed, justified **assessment plan**: which catalogue tests/datasets to run
in the Execution Engine and which control checklists to fill in the Controls
Engine. It replaces the manual "browse the catalogue and guess" step.

The **source of truth is the system card** generated by the qualification
module — specifically `Qualification.systemCardJson` (and, as supporting
evidence, the 13 `QualificationAnswer` rows). Not the free-text form fields.

```
Qualification (system card JSON)
        │
        ▼
   ┌─────────┐   MCP    ┌──────────────────┐
   │ Control Objectives  │◄────────►│ catalogue2 :8000  │  tests + datasets
   │ (agents)│◄────────►│ controls DB (ro)  │  checklists
   └────┬────┘          └──────────────────┘
        │ plan
        ├──► Execution Engine config (tests + datasets + plugin configs)
        └──► Controls Engine (checklist selection)
```

## 2. Inputs (exact shapes, verified against running code)

### 2.1 System card JSON — the primary source

`GET http://localhost/qualification/api/qualifications/{id}/system-card.json`
(route: `aisc/apps/qualification/src/app/api/qualifications/[id]/system-card.json/route.ts`;
404 if qualification unknown, 409 if card not generated yet).

Fields the service consumes:

| Field | Type | Used for |
|---|---|---|
| `classification.sectors` | `[{...}]` labels | sector matching (e.g. *Finance and insurance*) |
| `classification.target_systems` | `[{category, subcategory}]` | AI-type matching (e.g. *Tabular Classification & Regression*, *RAG*) |
| `findings[]` | `{title, article, summary, points, references}` | per-article evidence; maps to checklist articles and test dimensions |
| `open_issues[]` | strings, each prefixed `Article N: ...` | **highest-priority matching signal** — explicit gaps that selected tests/checklists must cover |
| `overview`, `description`, `target_use_case`, `target_users` | text | context for the proposer agents |
| `system_name`, `system_version`, `qualification_id` | identity | plan metadata |

The DB rows also carry `targetSystemTags` (slugs like
`tabular-structured-data:tabular-classification-regression`) and `sectorTags`
(`finance-and-insurance`) — the slug forms of `classification`. The service uses
the slug form for deterministic matching and the label form for prompts.

### 2.2 Catalogue (tests + datasets)

catalogue2 backend, FastAPI on `:8000`:

- `GET /tool/?detailed=true` — tools with `tags`, `metadata`, `metrics`, `questions`
- `GET /tags/` — full tag vocabulary; tag `section` ∈ {`license`, `ai_type`, `dimension`, `type`, `sector`, `testing_subdim`, `controls_subdim`, `source`}
- `GET /tags/{id}/tool/` — tools by tag

Matching-relevant tool fields: `tags[]` (sector + ai_type + dimension slugs),
`metadata.target_legal_requirements`, `metadata.control_topic`, `description`,
`status`/`completion_status`.

### 2.3 Control checklists

`aisc/apps/controls` Prisma models (`Checklist`, `Question`, `Source`).
Matching-relevant fields: `controlTopic`, `description`, `regulationIds`,
`countryIds`, `questions[].article`, `questions[].category`.

**There is no REST API** — the service reads the controls Postgres
**read-only** (same instance the demo seed writes to:
`postgresql://...@postgres:5432/controls`), via its own SQLAlchemy models
mirroring the three tables it needs. A dedicated read-only DB role is created
by the service's setup script.

### 2.4 Tag vocabulary mapping

Qualification target-system slugs are `category:subcategory`
(`natural-language-processing:question-answering`); catalogue `ai_type` tags
are category-level slugs (`natural-language-processing`,
`agents-and-agentic-systems`). Deterministic rule:

```
qual_tag.split(":")[0]  →  catalogue ai_type slug   (exceptions in a static map)
qual sectorTags         →  catalogue sector slugs   (already identical)
```

A static exception map (`control objectives/matching/tag_map.py`) handles the cases where
the prefix doesn't equal the catalogue slug (e.g.
`knowledge-retrieval:` → `knowledge-and-retrieval`). The map is **validated at
startup** against `GET /tags/`: any unmapped qualification category logs a
warning and falls through to the LLM matcher rather than silently dropping.

## 3. Stack

| Layer | Choice | Why |
|---|---|---|
| Service | **Python 3.12 + FastAPI + SQLAlchemy + pydantic** | Same stack as catalogue2; all business logic stays in Python (readable/debuggable by you) |
| Agents | **Anthropic Python SDK** (`anthropic`), model `claude-opus-4-8` (env-configurable `CONTROL_OBJECTIVES_MODEL`) | Tool runner + MCP helpers + structured outputs out of the box; adaptive thinking |
| Data access for agents | **MCP servers (Python `mcp` SDK / FastMCP)**, consumed via `anthropic.lib.tools.mcp.async_mcp_tool` + the SDK tool runner | The MCP boundary keeps agents on a read-only, typed surface; the same servers are reusable by other modules later |
| Persistence | Postgres (existing instance), new database `control_objectives` | Same ops pattern as qualification/controls |
| Frontend | **FastAPI + Jinja2 + HTMX**, served directly on `:8090` | Thinnest possible UI; whole module debuggable in Python; no Caddy edit needed. (Alternative: thin Next.js for visual consistency — see Open questions) |
| Packaging | `uv` + Dockerfile + compose service, joining the aisc compose network | Same pattern as catalogue2 backend |

## 4. MCP servers

One MCP server process (`control objectives/mcp_server.py`, stdio transport, spawned by the
orchestrator) exposing **read-only** tools. Tool results are compact JSON.

| Tool | Args | Returns |
|---|---|---|
| `get_system_card` | `qualification_id` | system card JSON (verbatim) + the 13 answers |
| `search_tests` | `ai_type_slugs?, sector_slugs?, dimension_slugs?, text_query?, type=test\|dataset` | list of `{tool_id, slug, name, description, tags, target_legal_requirements, metrics: [name]}` |
| `get_test` | `tool_id` | full tool detail incl. metadata + questions |
| `list_checklists` | `regulation_ids?, text_query?` | list of `{checklist_id, title, control_topic, description, source, articles: distinct questions[].article}` |
| `get_checklist` | `checklist_id` | checklist + questions (text, article, category) |

Design rules:

- Every ID an agent later cites **must** be resolvable via `get_test` /
  `get_checklist` — this is what the reviewer uses to kill hallucinated IDs.
- Search tools cap results (default 25) and return scores from the
  deterministic prefilter (§5.1) so agents see the ranking signal.

## 5. Matching pipeline

Deterministic first, agents second, reviewer last. The LLM never invents the
candidate set — it selects and justifies from a prefiltered list.

### 5.1 Deterministic prefilter (plain Python, no LLM)

For each catalogue tool and each checklist, compute a candidate score:

```
score = w1 * |tool.ai_type ∩ qual.ai_types|
      + w2 * |tool.sectors ∩ qual.sectors|
      + w3 * article_overlap(tool/checklist, system_card.findings + open_issues)
```

`article_overlap` extracts `Article N` references from
`metadata.target_legal_requirements` (tests) / `questions[].article`
(checklists) and from the system card's `references` + `open_issues`.
Everything with score > 0 (plus generic always-relevant items, e.g.
horizontal AI-Act checklists with no sector restriction) enters the candidate
set. Weights live in config; defaults `w1=3, w2=2, w3=2`.

### 5.2 Proposer agents (one per track, run in parallel)

- **TestProposer** — proposes tests + datasets from the candidate set.
- **ChecklistProposer** — proposes control checklists.

Each is an SDK tool-runner loop with the MCP tools, system card in the prompt,
adaptive thinking, and a **structured output** (pydantic via
`client.messages.parse`):

```python
class ProposedItem(BaseModel):
    item_id: str               # catalogue tool_id or checklist_id
    item_type: Literal["test", "dataset", "checklist"]
    priority: Literal["must", "should", "optional"]
    rationale: str             # ties the item to system-card evidence
    evidence: list[str]        # verbatim quotes/refs from the system card
    covers: list[str]          # article refs and/or open_issue indices covered

class Proposal(BaseModel):
    items: list[ProposedItem]
    coverage_gaps: list[str]   # open_issues/articles with no matching item
```

Prompting rules (from the system prompt, not re-asked per run):

- May only cite `item_id`s returned by MCP search/get tools.
- Every `open_issues[]` entry must appear in some item's `covers` or in
  `coverage_gaps` with a one-line reason.
- Datasets are proposed only alongside a test that consumes them.

### 5.3 Reviewer agent (quality gate)

Separate context (no shared history with proposers). Input: system card +
both proposals. It has the same read-only MCP tools and **must** verify every
cited ID with `get_test`/`get_checklist`. Structured verdict:

```python
class ItemVerdict(BaseModel):
    item_id: str
    verdict: Literal["accept", "reject", "revise"]
    reasons: list[str]         # e.g. id-not-found, weak-rationale, wrong-sector,
                               # duplicate-coverage, missing-dataset-pairing

class Review(BaseModel):
    verdicts: list[ItemVerdict]
    coverage_ok: bool          # all open_issues covered or explicitly gapped
    notes_for_revision: str
```

Loop: proposer → reviewer → (if any `revise`/`reject` or `coverage_ok=false`)
proposer revises with `notes_for_revision` → reviewer. **Max 3 rounds**, then
the plan is finalized with remaining issues flagged in the plan's `warnings`.

### 5.4 Cost/quality knobs

- Proposers and reviewer all default to `CONTROL_OBJECTIVES_MODEL` (`claude-opus-4-8`),
  `thinking={"type": "adaptive"}`, `output_config={"effort": "high"}`.
- Candidate sets and system card are placed before the cache breakpoint;
  per-round revision notes after it (prompt caching across review rounds).

## 6. Output: the Assessment Plan

Persisted in the `control_objectives` DB; one row per run, immutable once finalized.

```python
class AssessmentPlan(BaseModel):
    plan_id: str
    qualification_id: str
    system_name: str
    created_at: datetime
    status: Literal["draft", "reviewed", "finalized", "exported"]
    tests: list[ProposedItem]        # accepted only
    datasets: list[ProposedItem]
    checklists: list[ProposedItem]
    coverage: dict[str, list[str]]   # article/open_issue -> [item_id]
    gaps: list[str]
    warnings: list[str]
    review_rounds: int
    transcript_ref: str              # stored agent transcripts (auditability)
```

Exports (the "control objectives forks into both" arrows on the homepage):

- **Execution Engine**: `POST` of the selected tests + datasets as an engine
  configuration draft (exact endpoint TBD against the webapp backend API —
  Open question Q3).
- **Controls Engine**: creates `Submission` drafts (status `Draft`) for the
  selected checklists, via the same direct-DB pattern the demo seed uses.

## 7. Control Objectives service API

| Method, path | Purpose |
|---|---|
| `GET /control-objectives/api/qualifications` | proxy-list qualifications (id, name, has card) |
| `POST /control-objectives/api/plans` | `{qualification_id}` → start a run (async job), returns `plan_id` |
| `GET /control-objectives/api/plans/{id}` | plan + status (poll target for the UI) |
| `GET /control-objectives/api/plans/{id}/events` | SSE progress stream (prefilter done, proposer round n, review n, finalized) |
| `POST /control-objectives/api/plans/{id}/finalize` | human accepts (after optional manual deselect) |
| `POST /control-objectives/api/plans/{id}/export` | push to execution engine + controls |

Runs execute in a background task (FastAPI `BackgroundTasks` initially; move to
the existing celery/rabbit infra only if runs exceed request-lifetime limits).

## 8. UI (thin)

Three screens, HTMX over the API above:

1. **Pick qualification** — list with "has system card" badge; cardless ones are disabled with a "generate the system card first" hint.
2. **Run view** — live progress (SSE), then the plan: items grouped by track, each with rationale, evidence quotes, priority, reviewer verdict; coverage matrix (articles/open_issues × selected items); gaps highlighted. Checkboxes to deselect before finalizing.
3. **Export confirmation** — what was created where, with deep links into the webapp and controls.

## 9. Testing (TDD — tests first, throughout)

- **Matching prefilter**: pure-function unit tests; fixtures from the real MCAS seed (`demo/seed/qualification/microcredit.sql` system card) + a frozen snapshot of `tools_seed.json` and `controls_seed.json`. Golden expectations for the MCAS candidate set.
- **Tag map**: test that every `category` prefix in the qualification taxonomy resolves to an existing catalogue tag slug (run against the seed vocabulary).
- **MCP tools**: contract tests against a docker-compose test stack (catalogue2 + controls DB seeded); each tool's response validated against its pydantic schema.
- **Agents**: schema-level tests with mocked Anthropic client (proposal/review parsing, revision-loop control flow, max-rounds cutoff, hallucinated-ID rejection path). One opt-in live test (`CONTROL_OBJECTIVES_LIVE_TESTS=1`) running the full MCAS plan and asserting structural invariants (every open_issue covered or gapped; all IDs resolvable) — not exact item lists.
- **API**: FastAPI TestClient tests per endpoint.

## 10. Repository layout

```
control objectives/
  SPEC.md
  pyproject.toml            # uv
  docker-compose.control objectives.yml # joins aisc network; Caddy route /control-objectives
  src/control-objectives/
    api/                    # FastAPI routes + SSE
    matching/               # prefilter, tag_map, article extraction
    mcp_server.py           # read-only MCP tools
    agents/                 # proposer, reviewer, orchestrator loop
    clients/                # catalogue HTTP, qualification HTTP, controls ro-DB
    models/                 # pydantic + SQLAlchemy (plan persistence)
    ui/                     # Jinja2 templates + HTMX
  tests/
```

## 11. Non-goals (v1)

- Editing the catalogue or checklists (read-only).
- Auto-running tests after export (engines own execution).
- Auth (none of the sibling modules have it in dev).
- Multi-qualification batch runs.

## 12. Open questions (need your call before coding)

- **Q1 — Frontend**: FastAPI+Jinja2+HTMX (all-Python, recommended) or a thin Next.js app for visual consistency with qualification/controls? (Both are additive.)
- ~~Q2 — Controls access~~: resolved by §0 — direct read-only DB role; controls app untouched.
- **Q3 — Execution Engine export target**: which webapp-backend endpoint should receive the test/dataset configuration draft? (Needs a look at `aisc/apps/backend` API — I can map it next. If no suitable endpoint exists, v1 exports a config file the webapp can import, keeping the additive constraint.)
- **Q4 — Model**: default `claude-opus-4-8` for both proposers and reviewer; OK, or do you want the reviewer on a different model for independence?
- **Q5 — Human-in-the-loop point**: plan finalization is manual (spec'd); should export also require a second confirmation, or fire immediately on finalize?
