import React from "react";
import { createRoot } from "react-dom/client";
import ControlObjectives from "./ControlObjectives";

createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ControlObjectives />
  </React.StrictMode>
);
