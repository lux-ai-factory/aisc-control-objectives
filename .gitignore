.venv/
__pycache__/
*.egg-info/
/data/
.pytest_cache/

# local secrets (Anthropic API key for the Control Objectives service)
.env

# server-owned active-recommendation state (single-user demo persistence)
.control objectives_state.json

# standalone control objectives frontend build artifacts
frontend/node_modules/
frontend/build/
frontend/.env

# transient pasted screenshots
Pasted image.png

# BAF writes its log next to the process
application.log
